{
  "name": "pn-netlify-structured",
  "private": true,
  "type": "module",
  "dependencies": {
    "@netlify/blobs": "^8.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.34.2"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod --dir=public"
  }
}